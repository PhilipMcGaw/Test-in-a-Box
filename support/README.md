# Support Metadata

This directory contains shared machine-readable build information used by
the updater, About/version surfaces and run provenance.

`VERSION` remains at the repository root because it is a standard,
human-readable release marker.

`BUILD.json` records component and repository-layout versions that should
not be duplicated throughout application code.
