# Updater v1.1 patch

Changes:

- Fixes Stable/Development channel selection.
- Displays the selected update channel before launching PowerShell.
- Notes that the updater should locate the extracted project automatically
  rather than relying on archive folder names (recommended follow-up
  improvement for the main updater script).

Replace the repository's `update.bat` with this version.
